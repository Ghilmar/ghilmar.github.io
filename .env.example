# URL del endpoint del asistente IA (opcional).
# Dejar vacío para que el chat muestre el fallback en modo no disponible.
PUBLIC_AI_API_URL=

# Configuración de GitHub Pages (también se pueden definir como variables
# del repositorio en GitHub Actions: SITE_URL y BASE_PATH).
SITE_URL=https://TU-USUARIO.github.io
BASE_PATH=/TU-REPO